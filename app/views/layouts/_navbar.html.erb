<nav class="bg-gray-800 p-4 flex justify-between items-center">
  <div class="text-white font-bold text-lg">
    <%= link_to 'Project History Tracker', root_path, class: "hover:text-gray-300" %>
  </div>
  <div class="space-x-4">
    <%= link_to 'New Project', new_project_path, class: "text-gray-200 hover:text-white font-semibold" %>

    <% if user_signed_in? %>
      <%= link_to 'Log Out', destroy_user_session_path, data: { turbo_method: :delete }, class: "text-gray-200 hover:text-white font-semibold" %>
    <% else %>
      <%= link_to 'Log In', new_user_session_path, class: "text-gray-200 hover:text-white font-semibold" %>
    <% end %>
  </div>
</nav>